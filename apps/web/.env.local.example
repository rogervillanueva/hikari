# Copy this file to .env.local and fill in the secrets before running the dev server.
AZURE_SPEECH_KEY=PASTE_AZURE_SPEECH_KEY
AZURE_SPEECH_REGION=southcentralus
# Optional override if your Speech resource exposes a custom endpoint URL.
# AZURE_SPEECH_ENDPOINT=https://southcentralus.api.cognitive.microsoft.com/cognitiveservices/v1

# Enforce a maximum daily spend (in cents) for Azure TTS. 100 = $1.00
AZURE_TTS_BUDGET_CENTS=100

# Set the active provider for demo/dev usage.
NEXT_PUBLIC_PROVIDER=azure
NEXT_PUBLIC_TTS_VOICE=ja-JP-NanamiNeural
